[build]
  command = "pnpm install && pnpm next build"
  publish = ".next"
  # 指定项目根目录（如果项目在子目录中）
  base = "/"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200
